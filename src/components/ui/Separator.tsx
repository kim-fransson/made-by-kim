"use client";

import {
  Separator as RACSeparator,
  SeparatorProps,
} from "react-aria-components";

export const Separator = (props: SeparatorProps) => {
  return <RACSeparator {...props} className="border-white border my-16" />;
};
